spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

logging.level.org.springframework=info
logging.level.com.in28minutes.springboot.myfirstwebapp=info

spring.mvc.format.date=yyyy-MM-dd

#spring.datasource.url=jdbc:h2:mem:testdb
spring.jpa.defer-datasource-initialization=true

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/todos
spring.datasource.username=todos-user
spring.datasource.password=dummytodos
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#/connect todos-user@localhost:3306

#https://github.com/in28minutes/master-spring-and-spring-boot/tree/main/11-web-application#docker

#  1. First run this in terminal

#docker run --detach \
#  --env MYSQL_ROOT_PASSWORD=dummypassword \
#  --env MYSQL_USER=todos-user \
#  --env MYSQL_PASSWORD=dummytodos \
#  --env MYSQL_DATABASE=todos \
#  --name mysql \
#  --publish 3306:3306
#  mysql:8-oracle

# docker container ls   # --> show sql port must!

# 2. open mysqlshell
#connect todos-user@localhost:3306

# 3. enter password

#4 . \use todos

#5. \sql

#6. select * from todo;

# like normal


